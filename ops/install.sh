#!/usr/bin/env bash
# Phase 4 installer. Stub: real implementation publishes the three .NET
# services to a stable prefix, rewrites the plist __INSTALL_PREFIX__
# placeholders, copies them to ~/Library/LaunchAgents, and bootstraps them.
set -euo pipefail

echo "ops/install.sh: not yet implemented (Phase 4)"
exit 1
